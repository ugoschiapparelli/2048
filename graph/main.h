/**
*\file main.h
*\brief lancement du jeu en mode graphique
*/

/**
 *\fn void UpdateEvents(Input *in)
 *\brief On gère le tableau d'input in en l'initialisant, si ses valeurs sont modifié cela veut dire que l'on as pressé une touche
 *\param Input tableau des inputs
 */

void UpdateEvents(Input* in);

/**
 *\fn int main(void)
 *\brief On lance le jeu
 */

int main(void);
